
Execute (Open the search):
  execute "JiraVimSearchOpen type=story"

Then (Expect header to appear):
  Assert getline(1) =~# "Search for \"type=story\"", "No header found"
  Assert getline(2) ==# "========================", "No underlining found"

  /SEARCH RESULTS
  Assert getline(".") =~# "SEARCH RESULTS:", "No Search result section"
  normal! j
  Assert getline(".") ==# "---------------", "No underlining for search result section"

  normal! j
  Assert getline(".") =~# '\v^TEST\-\d+\s+.+$', "No results for search"
